name: Unit Test 

on:
  push:
    branches:
      - test-action
  pull_request:
    branches:
      - master

jobs:
  test-gws:
    runs-on: ubuntu-latest
  
    services:
      gws-postgis:
        image: gplates/postgis
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
          --name gws-postgis
        ports:
          - 5432:5432

      gws-redis:
        image: redis
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
          --name gws-redis
        ports:
          - 6379:6379

    steps:
      - 
        name: Check out repository code
        uses: actions/checkout@v4
      - 
        name: Run GWS
        run: |
          cp django/GWS/env.template django/GWS/.env
          docker run -d --name gws -v `pwd`:/gws --network ${{ job.container.network }} -p 18000:80 gplates/gws
      - 
        name: Sleep for 30 seconds
        run: sleep 30s
        shell: bash
      -
        name: Check gws
        run: |
          docker exec gws ls -l /var/log/apache2/error.log
          docker exec gws cat /var/log/apache2/error.log
          docker exec gws cat /startup.sh
          docker exec gws systemctl status apache2
          docker exec gws /startup.sh
      -
        name: Run testcases
        run: |   
          docker ps
          docker network ls
          docker inspect gws -f "{{json .NetworkSettings.Networks }}"
          
          cd test
          
          docker run --name test -v`pwd`:/workspace --network ${{ job.container.network }} --env GWS_SERVER_URL=http://gws:80 gplates/gws wget http://gws:80
          docker run --name test -v`pwd`:/workspace --network ${{ job.container.network }} --env GWS_SERVER_URL=http://gws:80 gplates/gws /workspace/test-server.sh
      