---
title: "Use GWS with C#"
metaTitle: "Use GWS with C#"
metaDescription: "This page demonstrates how to use GWS with the C# programming language."
---

The code snippet below shows how the GPlates Web Service can be used with C#.

```csharp

    string sURL;
    sURL = "https://gws.gplates.org/reconstruct/coastlines/?time=50";

    WebRequest wrGETURL;
    wrGETURL = WebRequest.Create(sURL);
    
    Stream objStream;
    objStream = wrGETURL.GetResponse().GetResponseStream();

    StreamReader objReader = new StreamReader(objStream);
    objReader.ReadLine();

```